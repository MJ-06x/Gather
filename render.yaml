services:
  - type: web
    name: gathered-family-photos
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --threads 4 --timeout 120 start:app
    disk:
      name: gathered-storage
      mountPath: /opt/render/project/src
      sizeGB: 10